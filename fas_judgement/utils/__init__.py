"""
Utils Package
-------------
WHY: Utilities are thin helpers with no business logic — they belong outside
     any domain layer. Examples: string sanitisation, URL validation, external
     API thin wrappers.

LAYER: Infrastructure utilities — may be called from any layer.
"""
