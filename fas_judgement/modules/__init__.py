"""
Modules Package
---------------
WHY: Modules are vertical slices of functionality (DDD "bounded contexts").
     Each module owns its own routes, services, models, and data.

     Current modules:
       - ai_security: Single-shot attack scanning + multi-turn attack sessions

LAYER: Application modules — each module is self-contained.
"""
